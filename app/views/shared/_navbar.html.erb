<nav class="bg-white shadow-lg fixed bottom-0 left-0 w-full z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <!-- Logo/Brand -->
      <div class="flex items-center">
        <%= link_to root_path, class: "flex-shrink-0 flex items-center" do %>
          <span class="text-xl font-bold text-gray-800">LiveBeat</span>
        <% end %>
      </div>

      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center space-x-4">
        <%= link_to "Events", scheduled_events_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>
        <%= link_to "Venues", venues_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>
        <%= link_to "About", pages_about_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>
        <%= link_to "Contact", pages_contact_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>

        <!-- User Authentication -->
        <div class="ml-4">
          <% if user_signed_in? %>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium" %>
          <% end %>
        </div>
      </div>

      <!-- Mobile menu button -->
      <div class="md:hidden flex items-center">
        <button id="mobile-menu-button" class="text-gray-600 hover:text-gray-900 focus:outline-none focus:text-gray-900">
          <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu -->
  <div id="mobile-menu" class="hidden md:hidden">
    <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3 bg-white border-t">
      <%= link_to "Events", scheduled_events_path, class: "text-gray-600 hover:text-gray-900 block px-3 py-2 rounded-md text-base font-medium" %>
      <%= link_to "Venues", venues_path, class: "text-gray-600 hover:text-gray-900 block px-3 py-2 rounded-md text-base font-medium" %>
      <%= link_to "About", pages_about_path, class: "text-gray-600 hover:text-gray-900 block px-3 py-2 rounded-md text-base font-medium" %>
      <%= link_to "Contact", pages_contact_path, class: "text-gray-600 hover:text-gray-900 block px-3 py-2 rounded-md text-base font-medium" %>

      <!-- Mobile User Authentication -->
      <div class="border-t pt-2 mt-2">
        <% if user_signed_in? %>
          <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "text-gray-600 hover:text-gray-900 block px-3 py-2 rounded-md text-base font-medium w-full text-left" %>
        <% else %>
          <%= link_to "Login", new_user_session_path, class: "text-gray-600 hover:text-gray-900 block px-3 py-2 rounded-md text-base font-medium" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const mobileMenuButton = document.getElementById('mobile-menu-button');
  const mobileMenu = document.getElementById('mobile-menu');

  mobileMenuButton.addEventListener('click', function() {
    mobileMenu.classList.toggle('hidden');
  });
});
</script>
